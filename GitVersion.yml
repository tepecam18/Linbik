mode: Mainline
tag-prefix: ''

assembly-versioning-scheme: MajorMinorPatch

branches:
  main:
    regex: ^main$
    increment: Minor       # main merge → minor artsın
    tag: ''
  develop:
    regex: ^dev$
    increment: Inherit       # develop merge → patch artsın
    tag: ''
  feature:
    regex: ^feature\/.*$
    increment: Inherit     # default davranış (istersen Patch de diyebilirsin)
    tag: ''
  release:
    regex: ^release$
    increment: Major
    tag: ''